import { useState } from 'react';
import Link from 'next/link';
import MainContainer from '../components/MainContainer';

const NotFound = () => {
    return <MainContainer keywords={'users'}>Not Found !!</MainContainer>;
};

export default NotFound;
